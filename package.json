{
  "name": "whatsapp-web-plus",
  "version": "2.6.66",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run build && node test_privacy_filter.js && node test_accessibility_runtime.js && node test_settings_state.js && node test_settings_menu.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
