{
  "name": "whatsapp-web-plus",
  "version": "2.6.71",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "build:debug": "node build-debug.mjs",
    "test": "npm run build && npm run build:debug && node test_privacy_filter.js && node test_accessibility_runtime.js && node test_settings_state.js && node test_settings_menu.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
